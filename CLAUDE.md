## 1. 이 환경에 대해

| 항목 | 값 |
| --- | --- |
| 작업 디렉터리 | 전반부 `~/bootcamp-day3-egfr` / 후반부 `~/bootcamp-day3-pnck` |
| 모델 | `claude-sonnet-5` (변경 금지 — 예산 산정 기준) |
| GPU | 로컬에 없고, 있어도 사용하지 않습니다. 학습·생성 작업은 **Colab T4**로 위임 |
| 로컬에서 할 일 | 데이터 수집·정제·분할, 결과 비교, 문서 작성 |
| Colab에서 할 일 | GPU가 필요한 학습·생성만 |

---

## 2. Colab 연결

`/mcp`로 colab 확인. 없으면 uv를 이용하여 colab-mcp 설치

**연결됐다고 판단하는 기준**: `torch.cuda.is_available()`가 `True`이고 디바이스명이 출력될 것. 연결 표시만으로는 미검증입니다.

**GPU를 못 받았을 때**

- **CPU로 대체 금지.** 조건이 달라져 비교가 무효가 됩니다. 배치·에폭 임의 축소도 금지.
- **2분 간격 2회까지만 재시도**, 그 뒤 사람에게 보고. 대기 중 로그 반복 조회 금지.
- 브라우저 조작(노트북 실행·재인증)이 필요하면 에이전트가 해결할 수 없습니다. 즉시 요청하십시오.

**런타임이 끊기면** 설치 패키지와 `/content` 파일이 모두 사라집니다. 그래서 **GPU 작업이 끝나는 즉시 산출물을 로컬로 복사하고 `data/datalist.md`에 기록**하십시오. 나중에 한 번에 가져오지 마십시오.

**보고 형식**: 무엇을 시도했고, 무엇이 실패했고, 지금 어느 단계인지.

---

## 3. 세션이 끊겼을 때 — 재개 절차

**처음부터 다시 시작하지 마십시오.** 다음 순서로 현재 지점을 파악합니다.

1. `data/datalist.md`를 읽습니다. 여기에 지금까지 만들어진 산출물의 경로·설명·shape이 기록되어 있습니다.
2. 기록된 파일이 실제로 존재하는지 확인합니다 (`ls`, 파일 크기).
3. 기록에는 있는데 파일이 없거나, 파일은 있는데 기록이 없으면 **그 단계부터** 재개합니다.
4. 재개 지점을 사람에게 먼저 보고하고 승인을 받은 뒤 진행합니다.

이미 완료된 전처리나 학습을 다시 실행하는 것은 비용과 시간의 낭비이며, seed가 재적용되면서 결과가 달라질 수 있습니다.

---

## 4. 경로 및 저장 규칙

```
data/
  raw/            원본 데이터
  processed/      정제·분할된 데이터
  datalist.md     ← 산출물 대장 (매 단계 갱신 필수)
models/           학습된 가중치
outputs/          보고서·표·그림
```

- 모든 산출물은 위 경로 규칙을 따릅니다.
- 파일을 만들 때마다 `data/datalist.md`에 **경로 · 설명 · shape · 생성 시각**을 한 행 추가합니다.
- 기존 파일을 덮어쓰지 않습니다. 재실행이 필요하면 사람에게 먼저 확인합니다.

---

## 5. 기본 작업 원칙

- 숫자는 **실제 실행 결과**에서 가져옵니다. 추정하거나 기억으로 채우지 않습니다.
- 실행하지 않은 것을 했다고 서술하지 않습니다.
- 실패한 단계는 실패로 기록합니다. 조용히 우회하지 않습니다.
- 파일을 만들거나 바꾸는 작업 전에는 계획을 먼저 제시하고 승인을 받습니다.
- 오래 걸리는 작업(학습, 대량 생성)은 백그라운드로 제출하고 job id를 알린 뒤 대기합니다. 완료 전까지 로그를 반복 폴링하지 않습니다.

---

## 6. 하지 말아야 할 것

- `harness_guide.md`의 **하네스 제약**(스킬·서브에이전트 개수) 임의 수정
- 모델 변경 (`claude-sonnet-5` 고정)
- 사람 승인 없이 기존 산출물 덮어쓰기·삭제
- 전반부·후반부 작업 디렉터리 혼용

---

## 7. 현재 과제

- 전반부: `@harness_guide.md` 참조
- 후반부: `@validation_mode.md` 참조

이 파일에는 과제 내용을 적지 않습니다. 과제가 끝나도 이 파일은 그대로 남습니다.
